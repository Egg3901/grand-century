import type { GameData, World } from '../../shared/types';
import type { Rng } from '../rng';

export function runPoliticsMonthly(_world: World, _data: GameData, _rng: Rng): void {
  // TODO(M3): Implement reform legality, ruling party dynamics, and elections.
}
