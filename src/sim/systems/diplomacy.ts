import type { GameData, World } from '../../shared/types';
import type { Rng } from '../rng';

export function runDiplomacyMonthly(_world: World, _data: GameData, _rng: Rng): void {
  // TODO(M4): Implement alliance/CB/sphere AI and relation drift logic.
}
